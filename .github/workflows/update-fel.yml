name: Update Dolby Vision FEL Lists

on:
  workflow_dispatch:
  schedule:
    - cron: "17 3 * * *"

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.x"

      - name: Generate FEL lists
        run: python generate_fel.py

      - name: Commit updated lists
        run: |
          git config user.name "FEL List Bot"
          git config user.email "actions@users.noreply.github.com"

          git add fel-latest.json
          git add fel-catalogue.json
          git add fel-upcoming.json
          git add fel-all.json

          git diff --cached --quiet || \
            git commit -m "Update Dolby Vision FEL lists"

          git push
